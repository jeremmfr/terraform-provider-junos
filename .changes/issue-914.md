<!-- markdownlint-disable-file MD013 MD041 -->
ENHANCEMENTS:

* **resource/junos_bgp_group**: add `authentication_key_wo` write-only argument and its `authentication_key_wo_version` companion, to be able to use an ephemeral value for the MD5 authentication key without storing it in the Terraform state (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_bgp_neighbor**: add `authentication_key_wo` write-only argument and its `authentication_key_wo_version` companion, to be able to use an ephemeral value for the MD5 authentication key without storing it in the Terraform state (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_iccp**: add `authentication_key_wo` write-only argument and its `authentication_key_wo_version` companion, to be able to use an ephemeral value for the MD5 authentication key without storing it in the Terraform state (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_iccp_peer**: add `authentication_key_wo` write-only argument and its `authentication_key_wo_version` companion, to be able to use an ephemeral value for the MD5 authentication key without storing it in the Terraform state (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_ospf_area**: add `authentication_simple_password_wo` write-only argument in `interface` block and `key_wo` write-only argument in `authentication_md5` block in `interface` block, with their `*_wo_version` companions, to be able to use ephemeral values without storing them in the Terraform state (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_ospf_area**: `key` argument in `authentication_md5` block in `interface` block is now optional, one of `key` or `key_wo` is required
* **resource/junos_rip_neighbor**: add `authentication_key_wo` write-only argument and `key_wo` write-only argument in `authentication_selective_md5` block, with their `*_wo_version` companions, to be able to use ephemeral values for the authentication keys without storing them in the Terraform state, `key` argument in `authentication_selective_md5` block is now optional and one of `key` or `key_wo` is required (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_security_ike_gateway**: add `client_password_wo` write-only argument in `aaa` block and its `client_password_wo_version` companion, to be able to use an ephemeral value for the AAA client password without storing it in the Terraform state (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_security_ike_policy**: add `pre_shared_key_hexa_wo` and `pre_shared_key_text_wo` write-only arguments with their `*_wo_version` companions, to be able to use ephemeral values for the preshared key without storing it in the Terraform state (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_security_ipsec_vpn**: add `authentication_key_hexa_wo`, `authentication_key_text_wo`, `encryption_key_hexa_wo` and `encryption_key_text_wo` write-only arguments in `manual` block, with their `*_wo_version` companions, to be able to use ephemeral values for the manual security association keys without storing them in the Terraform state (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_services**: add `url_parameter_wo` write-only argument in `security_intelligence` block, `primary_client_secret_wo` and `secondary_client_secret_wo` write-only arguments in `connection` block in `identity_management` block in `user_identification` block, with their `*_wo_version` companions, to be able to use ephemeral values without storing them in the Terraform state, `primary_client_secret` argument is now optional and one of `primary_client_secret` or `primary_client_secret_wo` is required (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_snmp_v3_usm_user**: add `authentication_key_wo`, `authentication_password_wo`, `privacy_key_wo` and `privacy_password_wo` write-only arguments with their `*_wo_version` companions, to be able to use ephemeral values without storing them in the Terraform state (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_system_login_user**: add `encrypted_password_wo` and `plain_text_password_wo` write-only arguments in `authentication` block, with their `*_wo_version` companions, to be able to use ephemeral values for the user password without storing it in the Terraform state (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_system_radius_server**: add `secret_wo` and `preauthentication_secret_wo` write-only arguments with their `*_wo_version` companions, to be able to use ephemeral values for the shared secrets without storing them in the Terraform state, `secret` argument is now optional and one of `secret` or `secret_wo` is required (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_system_root_authentication**: add `encrypted_password_wo` and `plain_text_password_wo` write-only arguments with their `*_wo_version` companions, to be able to use ephemeral values for the root password without storing it in the Terraform state (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
* **resource/junos_system_tacplus_server**: add `secret_wo` write-only argument and its `secret_wo_version` companion, to be able to use an ephemeral value for the shared secret without storing it in the Terraform state (Partial fix [#914](https://github.com/jeremmfr/terraform-provider-junos/issues/914))
