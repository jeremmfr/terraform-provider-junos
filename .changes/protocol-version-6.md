<!-- markdownlint-disable-file MD013 MD041 -->
NOTES:

* **Terraform 1.0 or later is now required**: the provider is now served with version 6 of the Terraform plugin protocol instead of version 5, to be able to use nested attributes in resource schemas, so the registry won't offer new releases of the provider to an older Terraform CLI
